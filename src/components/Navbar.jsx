import { useState } from 'react';
import { NavLink, Link } from 'react-router-dom';
const links=[{label:'Home',to:'/'},{label:'About',to:'/about'},{label:'Services',to:'/services'},{label:'Our Work',to:'/work'},{label:'Contact',to:'/contact'}];
export default function Navbar(){const[open,setOpen]=useState(false);return <header className="site-header"><div className="container nav-shell"><Link to="/" className="brand" onClick={()=>setOpen(false)} aria-label="Falcon Academic Consultancy home"><span className="brand-mark">F</span><span className="brand-copy"><strong>Falcon</strong><small>Academic Consultancy</small></span></Link><button className={`menu-toggle ${open?'is-open':''}`} aria-label="Toggle navigation" aria-expanded={open} onClick={()=>setOpen(v=>!v)}><span/><span/><span/></button><nav className={`site-nav ${open?'open':''}`} aria-label="Primary navigation">{links.map(link=><NavLink key={link.to} to={link.to} end={link.to==='/' } onClick={()=>setOpen(false)}>{link.label}</NavLink>)}<NavLink className="nav-cta" to="/contact" onClick={()=>setOpen(false)}>Free Consultation</NavLink></nav></div></header>}
